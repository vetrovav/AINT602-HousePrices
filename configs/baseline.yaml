configs/baseline.yamldataset: iris

model:
  type: random_forest
  n_estimators: 100
  max_depth: 3
  random_state: 42

split:
  test_size: 0.2
  random_state: 42

output:
  model_path: models/iris_random_forest.joblib
